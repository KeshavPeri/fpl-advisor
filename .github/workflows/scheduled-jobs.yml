name: Scheduled jobs

# Ticket #10 — the scaffold for the nightly write path. Today this runs only
# scripts/heartbeat.ts (no business logic yet, see the ticket); ingest jobs
# land on top of this same trigger in later tickets.

on:
  schedule:
    # 17:45 UTC daily. FPL-Core-Insights refreshes at 07:30 and 17:30 UTC, so
    # this is anchored shortly after the later of the two refreshes.
    # GitHub Actions cron is always UTC. Asia/Singapore is UTC+8 year-round
    # (no DST), so 17:45 UTC is 01:45 the FOLLOWING day in Singapore — this
    # schedule crosses the date line. Do not "fix" this cron to look like a
    # Singapore-evening time without re-deriving the UTC value; a Singapore
    # evening (e.g. 20:00 SGT) is 12:00 UTC the SAME day, a different line
    # entirely.
    - cron: '45 17 * * *'
  workflow_dispatch: {}

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run heartbeat
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: npx tsx scripts/heartbeat.ts

      - name: Run FPL-Core-Insights ingest
        # Defaults to CORE_INSIGHTS_SEASON's own DEFAULT_SEASON (2025-2026,
        # last season) when the env var below is unset -- kept explicit here
        # so the two ingest steps read the same at a glance.
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          CORE_INSIGHTS_SEASON: '2025-2026'
        run: npx tsx scripts/ingest-core-insights.ts

      - name: Run FPL-Core-Insights ingest (current season)
        # Ticket #113. Same job, same logic, second season -- last season
        # remains the prior for src/lib/projection/rates.ts and
        # defconRate.ts's two-stage shrinkage; this step is what lets a
        # player's actual 2026/27 form reach the model at all. A season
        # directory the source has not published yet is a normal,
        # non-failing state for this job (see ingest-core-insights.ts's own
        # header) -- this step is expected to log that plainly and exit 0
        # for however long that remains true early in the season.
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          CORE_INSIGHTS_SEASON: '2026-2027'
        run: npx tsx scripts/ingest-core-insights.ts

      - name: Run FPL ingest
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: npx tsx scripts/ingest-fpl.ts

      - name: Ingest match odds
        # Ticket #238. Runs after the FPL ingest step above, which populates
        # the teams/fixtures tables this job resolves fixture_ids against --
        # and before "Project points", which is the job that actually reads
        # fixture_odds. ODDS_API_KEY is already live in the repository
        # secrets (owner-provisioned, 15 Sept 2026); reading it here is not a
        # new-credential action.
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: npx tsx scripts/ingest-match-odds.ts

      - name: Project points
        # Ticket #33. Runs after the FPL ingest step above (needs fresh
        # players/teams/fixtures/gameweeks) and after the FPL-Core-Insights
        # ingest step above that (needs player_match_stats and teams.elo).
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: npx tsx scripts/project-points.ts

      - name: Resolve model cache key (gbm-v1)
        # Ticket #265 (R2-T2). Computes the actions/cache key ahead of the "Project points
        # (gbm-v1)" step below: VAASTAV_SHA (pinned, from model/fpl_model/sources.py) plus
        # FPL-Core-Insights' current `main` sha, resolved fresh here -- so a moved upstream Core
        # `main` invalidates the cache instead of silently reusing yesterday's files.
        # model/fpl_model/fpl_api.py's resolve_core_ref() resolves the same sha again inside the
        # job itself (that is the one sources.load_history actually uses -- never the literal
        # 'main', see that function's docstring); this step exists only so the cache key can see
        # it in advance. Runs on the runner's preinstalled python3, before Python is even
        # otherwise set up for this job.
        id: model-cache-key
        run: |
          vaastav_sha=$(grep -oP "VAASTAV_SHA = '\K[0-9a-f]+" model/fpl_model/sources.py)
          core_sha=$(curl -sS -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/olbauday/FPL-Core-Insights/commits/main \
            | python3 -c "import json,sys; print(json.load(sys.stdin)['sha'])")
          echo "key=${vaastav_sha}-${core_sha}" >> "$GITHUB_OUTPUT"

      - name: Set up Python (gbm-v1)
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache model data (gbm-v1)
        uses: actions/cache@v4
        with:
          path: model/.cache
          key: model-cache-${{ steps.model-cache-key.outputs.key }}

      - name: Project points (gbm-v1)
        # Ticket #265 (R2-T2). Runs `gbm-v1` (#263, offline-gated: 5-GW Spearman 0.589 vs 0.453
        # for points-per-appearance, model/reports/eval-latest.md) and writes its rows into
        # player_projections NEXT TO baseline-v1 (model_version='gbm-v1', same table, same PK --
        # a different model_version never collides with the "Project points" step above).
        # THIS DOES NOT CHANGE RECOMMENDATIONS: config/projection-model.json stays on
        # 'baseline-v1' until the owner flips it after checking the first run (see the ticket's
        # "Post-merge owner check"; "Emit projections CSV" below still reads only `active`,
        # which this step never touches).
        #
        # continue-on-error: true -- the baseline-v1 path (this step's own failure, and every
        # step after it) must still run even if this step fails. A failure is still visible as a
        # red step here, and (when Supabase itself is reachable) as a job_runs row with
        # status='failure' -- model/fpl_model/live.py's own top-level error handling, mirroring
        # scripts/heartbeat.ts's main().catch(...).
        working-directory: model
        continue-on-error: true
        timeout-minutes: 20
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: |
          pip install -r requirements.txt
          python -m fpl_model.live

      - name: Sync squad from FPL
        # Ticket #14. Runs after the FPL ingest step above, which populates
        # the gameweeks and players tables this job reads from. FPL_ENTRY_ID
        # is optional at the secrets level: scripts/sync-squad.ts exits zero
        # with a named message and makes no request if it is unset, so this
        # step is safe to run before the entry id is known.
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          FPL_ENTRY_ID: ${{ secrets.FPL_ENTRY_ID }}
        run: npx tsx scripts/sync-squad.ts

      - name: Emit projections CSV
        # Ticket #34 (product-brief.md §6c — "the seam"). Runs after the
        # "Project points" step above, which populates player_projections
        # this job reads from. Writes ./out/projections.csv (the
        # PROJECTIONS_CSV_PATH default) — a plain file on the runner. This
        # job does not check out, install or invoke the solver; item 12 is
        # what actually feeds this file to it.
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: npx tsx scripts/emit-projections-csv.ts

      - name: Upload projections CSV artifact
        # Runs even if the step above failed (if: always()), so a partial or
        # missing file can still be inspected/reported on — but
        # if-no-files-found: warn means a missing file only warns, never
        # fails this step and never fabricates an empty artifact. The
        # workflow's overall failure (if "Emit projections CSV" failed) is
        # never masked by this step: a red job here means that earlier step
        # failed, not that the upload itself did.
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: projections-csv
          path: out/projections.csv
          if-no-files-found: warn
          retention-days: 14
