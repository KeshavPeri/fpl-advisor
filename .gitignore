# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code: the shared allowlist is committed; per-machine additions are not
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code worktree isolation scaffolding (not repo content)
.claude/worktrees/

# Local credentials for hand-run scripts (SUPABASE_URL / SUPABASE_SECRET_KEY).
# Never committed. Read with `npx tsx --env-file=.env scripts/<job>.ts`.
.env
.env.*

# Local output from hand-run scripts (learned-model report, etc.)
out/

# model/ (ticket #258): downloaded CSVs cached across runs, and Python build artefacts.
# model/reports/eval-latest.md is generated too, but it IS committed (the offline gate result).
model/.cache/
.venv/
__pycache__/

