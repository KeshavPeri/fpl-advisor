# CLAUDE.md

Stub — full version written in Phase 4 (Task 4.5), once the agent definitions exist. This is
what every fresh routine session reads first for context, since no run can resume a previous
one.

## Branch convention

All work happens on branches named `claude/ticket-<number>-<slug>`, e.g.
`claude/ticket-14-gameweek-deadline-countdown`. This matches Claude Code Routines' default push
restriction — `claude/`-prefixed branches are always accepted.

## More to come in Phase 4

- Board column names and what each means
- Where `decisions.md` lives and its two-section format
- The design-pass rule (baseline `frontend-design` + `design-reference.md` on normal tickets;
  Impeccable / emil-design-eng confined to polish tickets only)
- Links to `.claude/agents/analyst.md`, `builder.md`, `qa.md`
