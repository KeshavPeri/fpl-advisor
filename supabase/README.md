# Applying a Supabase migration — for Keshav

This repo does not run migrations automatically. `supabase/migrations/` holds
plain SQL files that an agent writes but never applies — applying anything to
the live Supabase project touches live data, which is an owner-only action
(Tier 1, `escalation.md`). Every migration file in this folder is idempotent:
running one twice is safe, so if you're ever unsure whether one already went
in, re-run it and check the notices.

## Steps

1. Open the [Supabase dashboard](https://supabase.com/dashboard) and select
   the `fpl-advisor` project.
2. In the left sidebar, click **SQL Editor**.
3. Click **New query**.
4. Open the migration file you want to apply from `supabase/migrations/` in
   this repo (they're timestamp-prefixed — apply them in filename order if
   you're catching up on more than one) and paste its full contents into the
   query editor.
5. Click **Run** (or press **Cmd/Ctrl + Enter**).

## How to tell it worked

- The editor prints `Success. No rows returned` after **Run** — that's the
  first signal.
- Go to **Table Editor** in the left sidebar. The tables the migration
  creates should now be listed (for the reference-schema migration:
  `teams`, `players`, `fixtures`, `gameweeks`).
- Click into one of the new tables and check the **RLS** toggle at the top —
  it should show as **Enabled**. Click **Policies** for that table — it
  should list exactly one policy, applying to `SELECT` for the `anon` role.
- If instead the editor shows a red error box, nothing was committed for that
  statement — read the error, it names the exact line. Re-running a
  migration file that partly failed is safe (every statement in it is
  guarded with `IF NOT EXISTS` / `DROP ... IF EXISTS`), so fix the underlying
  issue and click **Run** again rather than trying to hand-patch around it.

## Migrations applied so far

Newest last. Apply in filename order.

**Warning:** two migrations share the prefix `20260811180000` — they were generated by tickets
running in parallel on the same night. Alphabetical order happens to be correct here
(`player_match_stats_player_code` before `squad_state`, and they touch different tables), but do
not rely on that again. When two tickets in one batch both add a migration, check the timestamps.

| File | What it does | Applied to live? |
|---|---|---|
| `20260811100000_reference_schema.sql` | Creates `teams`, `players`, `fixtures`, `gameweeks` with read-only RLS for `anon`. No data. | **Yes — 11 Aug 2026.** Ticket #9. |
| `20260811130000_job_runs.sql` | Creates `job_runs` (job telemetry: name, status, started/finished, message, `details jsonb`) with read-only RLS for `anon`. Written by the Action's secret key, which bypasses RLS. | **Yes — 11 Aug 2026.** Ticket #10. |
| `20260811160000_table_grants.sql` | Grants `SELECT` to `anon` and `SELECT, INSERT, UPDATE` to `service_role` on all five tables, plus default privileges for future ones. `DELETE` deliberately withheld. Fixed `permission denied for table job_runs`. | **Yes — 11 Aug 2026.** Follow-up to #9 and #10. |
| `20260811170000_player_match_stats.sql` | Creates `player_match_stats` (per-match defensive and attacking stats from FPL-Core-Insights). No FK to `players` — FPL element ids are not stable across seasons. | **Yes — 11 Aug 2026.** Ticket #12. |
| `20260811180000_player_match_stats_player_code.sql` | Adds `player_code` to `player_match_stats` as the stable cross-season join key. | **Yes — 11 Aug 2026.** Ticket #22. |
| `20260811180000_squad_state.sql` | Creates `squads` and `squad_picks`, with RLS + GRANTs for `anon` (incl. DELETE). | **Yes — 11 Aug 2026.** Ticket #13. |
| `20260811190000_squad_api_sync_fields.sql` | Adds the sync-metadata columns #14's squad sync writes, and grants `squads`/`squad_picks` to `service_role`. | **Yes — confirmed applied 15 Aug 2026.** Ticket #14. Verified by checking `information_schema.columns` for `total_transfers`, `overall_points`, `overall_rank` and `chips_used` on `public.squads`. |
| `20260815120000_player_projections.sql` | Creates `player_projections` (one row per gameweek × player × model version: expected points, expected minutes, and the model's inputs and point components as `components jsonb`). RLS read-only for `anon`; `SELECT, INSERT, UPDATE` for `service_role`, no `DELETE`. | **Yes — applied 15 Aug 2026.** Ticket #33 (feature-list item 10). Confirmed by the first `project-points` run writing 2,935 rows. |
| `20260816090000_solver_output.sql` | Creates `solver_runs` (one row per solve: gameweek, solver status, objective, horizon, seconds, `config jsonb`) and `solver_picks` (one row per player per gameweek per solution, carrying `solution_index` so a multi-solution run needs no second migration). RLS read-only for `anon`; `SELECT, INSERT, UPDATE` for `service_role`, no `DELETE`. | **Yes — applied 16 Aug 2026.** Ticket #41 (feature-list item 12). Confirmed by the first `solver-run` workflow producing a stored solve. |
| `20260817090000_recommendations.sql` | Creates `recommendations` (one row per gameweek × `plan_index` 0–2: the transfer in/out or an explicit `is_roll`, captain and vice-captain, `starting_xi` and `bench_order` as `jsonb`, `hit_cost`, `gross_points` / `net_points` with pre-rounded whole-number twins, `confidence_band`, and a `coverage` jsonb flagging which named players the model has no history for) and `recommendation_reasons` (ordered reason lines, cascading on the parent). RLS read-only for `anon`; `SELECT, INSERT, UPDATE` for `service_role`, no `DELETE`. | **Yes — applied 17 Aug 2026.** Ticket #47 (feature-list item 13). Confirmed by a successful `solver-run` workflow after applying. |
| `20260818090000_notifications.sql` | Creates `notifications` — an append-only log of every Telegram send attempt: the gameweek, the recommendation and plan the message was built from, `send_kind`, `outcome`, the exact `message_text` sent, and Telegram's own status and error. RLS read-only for `anon`; `SELECT, INSERT` for `service_role` — **no `UPDATE`, no `DELETE`**, so append-only is a database guarantee. | **Yes — applied 18 Aug 2026.** Ticket #55 (feature-list item 14). Confirmed by a real Telegram message being received. |
| `20260818100000_player_match_stats_competition.sql` | Adds `competition` (indexed) to `player_match_stats`, parsed from the `match_id` slug at ingest so consumers can filter to Premier League matches only — ~18% of rows are cup and European games. **Does not add `team_goals_conceded`** — this row previously claimed it did, wrongly, from 18 Aug 2026 until ticket #125 corrected it on 27 Aug 2026; the file itself was checked directly and only ever added `competition`. `team_goals_conceded` was added later, by ticket #125's own migration below. | **Yes — applied 18 Aug 2026.** Ticket #54. Confirmed by `matchRowsWithCompetition` equalling `matchRowsWritten`, and by no player exceeding 38 Premier League matches. |
| `20260828090000_player_match_stats_team_goals_conceded.sql` | Adds `team_goals_conceded` (nullable, no default) to `player_match_stats` — the column clean sheets must be read from; the pre-existing `goals_conceded` is a goalkeeper-only stat (74% populated on goalkeeper rows, 1.1% on outfield rows). Populated by `scripts/ingest-core-insights.ts` on every row, existing and new, exactly as `competition` was back-stamped by #54 — no separate backfill. No new `GRANT` (table-level grants from #12 already cover it). See the file header for the bonus/BPS-absent-from-source finding recorded alongside this migration. | **Yes — applied 28 Aug 2026.** Ticket #125. |
| `20260819090000_notification_trigger.sql` | Adds `trigger` (`manual` / `deadline_24h` / `deadline_10h`) to `notifications`, plus a **partial unique index on `(gameweek_id, trigger)` where `outcome = 'sent'`** — so a double-send is impossible at the database level while a failed send stays retryable. | **Yes — applied 19 Aug 2026.** Ticket #59 (feature-list item 15). Confirmed by the preflight check's notifications check reading trigger state successfully. |
| `20260820100000_recommendations_delete_grant.sql` | Grants `DELETE` on `public.recommendations` to `service_role`, so a re-run that produces fewer distinct plans can remove the orphaned ones. Scoped to that one table; `recommendation_reasons` follows via `ON DELETE CASCADE`. See the file header for why this does not contradict the deliberate no-`DELETE` rule on ingested source data. | **Yes — applied 20 Aug 2026.** Ticket #60. Tier 1, owner-approved. |
| `20260821090000_prediction_log.sql` | Creates `prediction_log` (one row per gameweek × player × model version: the pre-deadline snapshot — `projected_points`, `projected_minutes`, `components`, `captured_at` — plus the post-lockdown settlement columns `actual_points`, `actual_minutes`, `settled_at`, `error`, all nullable so "not measured yet" and "a real zero" stay distinct). RLS read-only for `anon`; `SELECT, INSERT, UPDATE` for `service_role`, no `DELETE`. | **Yes — applied 22 Aug 2026.** Ticket #73 (feature-list item 23). |
| `20260823090000_recommendation_decisions.sql` | Creates `recommendation_decisions` — the append-only ledger of what Keshav actually did with a recommendation: `gameweek_id`, `plan_index`, `kind` (`'commit'` or `'override'`), `decided_at`, and a `snapshot` jsonb of what was committed (`is_roll`, transfer in/out, captain, vice-captain, `hit_cost`, `solver_run_id`). RLS read+insert for `anon` (this app has no auth, same shape as `squads`/`squad_picks`); `SELECT, INSERT` for `service_role` — **no `UPDATE`, no `DELETE`** to any role, so append-only is a database guarantee. Unique index on `(gameweek_id, plan_index, kind)` stops the same plan being decided twice under the same kind. | **Yes — applied 23 Aug 2026.** Ticket #84 (feature-list item 19). Verified locally by applying it twice against a Postgres 16 instance (idempotent) and exercising it as `anon`: insert and select succeed, a duplicate commit hits the unique index (23505), and update/delete are denied to both `anon` and `service_role`. Not yet re-confirmed against the live table after applying. |
| `20260827090000_feature_history.sql` | Creates `feature_history` — one row per (`season`, `gameweek_id`, `player_code`) carrying the CUMULATIVE Premier League match totals of that player's matches STRICTLY BEFORE that gameweek (`prior_matches`, `prior_minutes`, `prior_xg`, `prior_xa`, `prior_saves`, `prior_clearances`, `prior_blocks`, `prior_interceptions`, `prior_tackles`, `prior_recoveries`, `prior_team_goals_conceded`), keyed on `player_code` (never the per-season element id) so it stays meaningful across a season boundary. `gameweek_id` here is the plain gameweek number, not an FK to `public.gameweeks`. Raw totals only, no rates — see the file header. RLS read-only for `anon`; `SELECT, INSERT, UPDATE` for `service_role`, no `DELETE`. Populated by hand-run `scripts/build-feature-history.ts`, not by any workflow. | **Yes — applied 28 Aug 2026.** Ticket #121 (feature-list item 29, first slice). |
| `20260828100000_chip_advisories.sql` | Creates `chip_advisories` — the difference between a chip-enabled solve and the normal (chip-free) solve it is compared against. One row per (`gameweek_id`, `solution_index`, `chip_code`): `chip_gameweek_id` (the horizon gameweek the chip would be played in), `chip_enabled_objective`, `chip_free_objective`, `delta` (a `GENERATED` column: `chip_enabled_objective - chip_free_objective`), and `solver_run_id` (FK to `solver_runs`, the exact run it is compared against). Append-only, matching `solver_runs`. RLS read-only for `anon`; `SELECT, INSERT, UPDATE` for `service_role`, no `DELETE`. Never read by `recommendations`/`solver_picks`/the Telegram message — surfaced only on the chips screen as an advisory, never a "play this chip" instruction. | **Yes — applied 28 Aug 2026.** Ticket #126 (feature-list item 27). |
| `20260829090000_feature_history_position_and_defcon.sql` | Adds `element_type` (nullable smallint), `prior_defcon_qualifying_matches` and `prior_defcon_hits` (both nullable integer, no default) to `feature_history`, and `element_type` (nullable smallint) to `player_match_stats`. Closes the two `feature_history` gaps that were dropping 23% of the backtest population (no stored position, forcing a join to the live `players` table) and approximating the defensive-contribution hit rate from cumulative totals it cannot actually be recovered from. `element_type` is copied from the ingested season's own `players.csv` (via `player_match_stats.element_type`, populated by `scripts/ingest-core-insights.ts`), never from the live `players` table. The two defcon columns are raw per-match counts (qualifying matches, hits), not a rate — same "raw totals, never rates" reasoning as `feature_history`'s existing `prior_*` columns. No new `GRANT` on either table (table-level grants from earlier migrations already cover them). Populated by hand-run `scripts/build-feature-history.ts` (a full season rebuild) and `scripts/ingest-core-insights.ts` (a re-ingest); no separate backfill. | **Yes — applied 29 Aug 2026.** Ticket #146. |
| `20260831090000_team_and_opponent.sql` | Adds `team_code` and `opponent_team_code` (both nullable integer, no default) to `player_match_stats`, and `team_code` (nullable integer, no default) to `feature_history` — the substrate a follow-up ticket needs to make the backtest fixture-aware (today it projects every row under a neutral fixture). `team_code` matches `public.teams.code`, never an FPL team id (deltas.md D9) — populated from that season's own `players.csv` `team_code` column, never the live `players`/`teams` tables. `opponent_team_code` is derived from the `match_id` slug's two club-name segments given the player's own `team_code`, via a club-slug → `team_code` map built off that season's `teams.csv` `fotmob_name` column (`scripts/ingest-core-insights.ts`'s `buildClubCodeBySlug`/`resolveOpponentTeamCode`) — a slug that does not resolve to exactly two known clubs is left `NULL` and counted, by reason, in `job_runs.details`, never guessed. **Identity only — no fixture-difficulty derivation; that is a follow-up ticket.** No new `GRANT` on either table (table-level grants from earlier migrations already cover them). Populated by `scripts/ingest-core-insights.ts` (team + opponent) and `scripts/build-feature-history.ts` (team, copied through); no separate backfill. | **Yes — applied; confirmed 3 Sep 2026.** Ticket #167. Verified against `information_schema.columns` (both `player_match_stats` columns and `feature_history.team_code` present) and by 18,588 of 18,588 `feature_history` rows carrying `team_code`. |
| `20260901090000_teams_elo_stale_since.sql` | Adds `elo_stale_since` (nullable `timestamptz`, no default) to `teams`. `scripts/ingest-core-insights.ts` no longer nulls `teams.elo` when a season file has no value for a club (absent, blank, or malformed cell) — it PRESERVES the existing rating and sets this column instead, narrowing ticket #63's nulling rule, which had started firing on a normal source gap (the 2026-2027 file publishing `elo` empty for all 20 clubs) rather than only on a genuine departure from the competition. NULL means the rating is current; a timestamp means it's been preserved, not confirmed, since that moment — cleared back to NULL the next time a fresh CSV value updates `elo`. No new `GRANT` (table-level grants from `20260811160000_table_grants.sql` already cover `teams`). See the migration file's header for the full "because". | **Yes — applied; confirmed 3 Sep 2026.** Ticket #176. Verified against `information_schema.columns` — `public.teams.elo_stale_since` present. |
| `20260902090000_feature_history_recent_minutes.sql` | Adds `prior_recent_minutes` (nullable `integer[]`, no default) to `feature_history` — this player's minutes played in his most recent Premier League matches with a resolvable `player_code` (the SAME contributing-row set the existing `prior_*` totals use), strictly before `gameweek_id`, most recent first, capped at `RECENT_MATCH_COUNT` (5, exported from `src/lib/projection/minutes.ts`). Stores the true last-five-match window the live model already uses via `estimateMinutes()`, so a follow-up ticket can make `scripts/run-backtest.ts` read it instead of its own single-averaged-match reconstruction — **substrate only, no backtest number changes when this merges.** Not filtered by any minutes threshold (cameos count). No new `GRANT` (table-level grants from `20260827090000_feature_history.sql` already cover `feature_history`). Populated by hand-run `scripts/build-feature-history.ts` (a full season rebuild); no separate backfill. | **Yes — applied; confirmed 3 Sep 2026.** Ticket #181. Verified against `information_schema.columns` and by 18,588 of 18,588 `feature_history` rows carrying `prior_recent_minutes`; backtest report 9 reports 18,588 rows from the stored window and 0 from the averaged fallback. |
| `20260904090000_training_features.sql` | Creates `training_features` — point-in-time training substrate for the learned model (R6), one row per (`season`, `gameweek_id`, `player_code`), built on the same strictly-before discipline as `feature_history`, but admitting a row only when `prior_matches > 0` (no row-of-zeros for a debut gameweek, unlike `feature_history`) and storing several columns as genuinely computed rates/shares (`xg_rate_per90`, `xa_rate_per90`, `season_avg_minutes`) rather than raw totals — see the migration file's own header for the full "because" (Tier 2, logged HIGH-IMPACT). Also carries `prior_shots_on_target` (a NEW strictly-before total, since `feature_history` does not store shots on target), `prior_recent_minutes`/`prior_defcon_qualifying_matches`/`prior_defcon_hits`/`element_type`/`team_code`/`prior_matches` (copied through from `feature_history`), `opponent_team_codes` (this gameweek's own scheduled fixture(s) — schedule, not result; an array to cover a double gameweek), and `team_strength_matches`/`team_strength_goals_scored`/`team_strength_goals_conceded` (the player's own club's point-in-time record, reusing `scripts/run-backtest.ts`'s `buildTeamMatchRecords`/`computeTeamStrengthAsOf`/`buildClubFixtureSchedule`/`lookupClubFixtureSchedule` unmodified — also Tier 2, logged HIGH-IMPACT). RLS read-only for `anon`; `SELECT, INSERT, UPDATE` for `service_role`, no `DELETE`, `GRANT` in the same file. Populated by hand-run `scripts/build-training-features.ts`, not by any workflow. **No model is trained or evaluated by this migration or its job — substrate only; nothing user-visible changes.** | **Not yet applied.** Ticket #203 (feature-list items 30/31 reshaped, R6's first slice). |

Update this table by hand after you apply a migration, so the next person
(or the next overnight run reading this file) knows what state the live
database is actually in.

**Every migration that creates a table must also GRANT on it.** RLS and GRANTs are independent
gates and a query must pass both. A policy without a grant produces `permission denied for
table X`; a grant without a policy produces `new row violates row-level security policy`. The
first one cost a failed run on 11 Aug 2026. Read the error text — it tells you which gate closed.

**A migration file on `main` does not mean it has been applied.** Nothing in this repo can
apply one — that is owner-only (Tier 1). If a job fails with a missing-table error, check this
table first: the most likely cause is a merged migration that nobody ran.
